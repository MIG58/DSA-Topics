/***/
#include <stdio.h>
#include <stdlib.h>

void bubble(int *,int);

int main()
{
    int size,*ptr,i,j,temp;

    printf("Enter the size of array: ");
    scanf("%d",&size);
    ptr = (int *)malloc(sizeof(int)*size);

    printf("Enter the elements in array: ");
    for(i=0;i<m;i++)
    {
        scanf("%d",&(ptr+i));
    }



return 0;
}

void bubble(int *ar,int n)
{
    
}